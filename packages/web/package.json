{
  "private": true,
  "scripts": {
    "build": "shx mkdir -p dist/wasm && shx cp index.html dist/ && shx cp node_modules/coi-serviceworker/coi-serviceworker.min.js dist/ && shx cp ../../dist/index.js ../../dist/version.js dist/ && shx cp ../../dist/wasm/core.js ../../dist/wasm/core.wasm dist/wasm/",
    "setup-cert": "mkcert create-ca && mkcert create-cert",
    "serve": "serve dist -l 3000 --ssl-cert cert.crt --ssl-key cert.key"
  },
  "devDependencies": {
    "mkcert": "^3.2.0",
    "serve": "^14.2.6",
    "shx": "^0.4.0"
  },
  "dependencies": {
    "coi-serviceworker": "^0.1.7"
  }
}
